import 'package:flutter/material.dart';

class EditImageBarcode extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Image & Barcode'),
      ),
      backgroundColor: Colors.white,
    );
  }
}
